import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976",
    },
    secondary: {
      main: "#dc004e",
    },
  },
});

export default theme;
