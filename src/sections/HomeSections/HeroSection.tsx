const HeroSection = () => {
  const containerStyle: React.CSSProperties = {
    textAlign: "center",
  };

  return (
    <div style={containerStyle}>
      <h1>I am HeroSection</h1>
      <button>DonateBtn</button>
      <p>Img</p>
    </div>
  );
};

export default HeroSection;
